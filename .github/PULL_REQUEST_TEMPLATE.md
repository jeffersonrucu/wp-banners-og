## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem being solved, not the diff. -->

## Checklist

- [ ] `composer phpcs` passes
- [ ] `composer phpstan` passes
- [ ] `composer dist && wp plugin check dist/banners-og` reports no errors
- [ ] `CHANGELOG.md` updated
- [ ] Version bumped in `banners-og.php`, `config.php` and `readme.txt` (`composer check-version`), if this is a release

## Tested on

<!-- WordPress and PHP versions, and the browser if the change touches the generator. -->
