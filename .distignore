# Everything that is development tooling, not part of the plugin.
# Kept in sync with the export-ignore list in .gitattributes: this file feeds
# bin/build-dist.sh, that one cleans the archives GitHub generates on its own.
/.git
/.gitmodules
/.editorconfig
/.distignore
/.gitattributes
/.github
/.gitignore
/bin
/composer.json
/composer.lock
/dist
/node_modules
/phpcs.xml.dist
/phpstan.neon.dist
/tests
/vendor
/CHANGELOG.md
/CONTRIBUTING.md
/README.md
/SECURITY.md
